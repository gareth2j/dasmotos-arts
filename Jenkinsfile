pipeline {

    stages {

        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('sonarqube-localhost') {
                 
                }
            }
        }
    }
}